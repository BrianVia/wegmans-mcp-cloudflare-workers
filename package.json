{
  "name": "wegmans-mcp",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=22.18" },
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config ${WRANGLER_CONFIG:-wrangler.jsonc}",
    "check": "tsc --noEmit && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "hono": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
